# overnight.asia — 도메인 · 아임웹 연동 런북

오늘 안에 끝내는 순서입니다. **사람이 관리자 화면에서 눌러야 하는 지점은 6곳뿐**이고, 나머지는 이 저장소 파일 복붙으로 끝납니다.
각 단계 끝의 `→ 검증` 은 `./overnight-dns-check.sh` 로 자동 확인할 수 있습니다.

---

## 시작 전 확인 (2분)

- [ ] `overnight.asia` 를 **어디서 샀는지** — 가비아 / 후이즈 / Cloudflare / Namecheap / GoDaddy 중 어디인지
- [ ] 그 계정에 **로그인이 되는지** (DNS 관리 화면까지 들어가지는지)
- [ ] 아임웹 요금제가 **유료 플랜인지** — ⚠️ 무료 플랜은 개인 도메인 연결이 안 됩니다. 결제하셨다니 통과일 겁니다
- [ ] 아임웹 사이트가 **"오픈" 상태인지** (비공개 상태면 도메인을 연결해도 접속이 막힙니다)

---

## STEP 1 — 아임웹 템플릿 선택 · 페이지 골격 만들기

**👤 사람이 하는 곳 ①**

1. 아임웹 관리자 → `디자인 > 디자인 관리 > 템플릿 변경`
2. **이벤트 / 랜딩 / 원페이지** 계열에서 **다크 배경을 지원하는** 템플릿 선택
   - 사이드바 없는 **원페이지형**을 고르세요. 히어로가 전체 폭으로 나와야 합니다
   - 상단 고정 헤더(스티키)가 되는 템플릿이면 더 좋습니다
3. `페이지 관리 > 페이지 추가`
   - 페이지 이름: `단양`
   - 페이지 주소: `danyang`  → 최종 주소가 `overnight.asia/danyang` 이 됩니다
   - 메뉴 노출: **끄기**
4. 페이지 배경색을 `#080b11` 로 지정 (`디자인 > 색상`)

> 왜 `/danyang` 인가: 2027 문경이 붙을 때 URL을 갈아엎지 않기 위해서입니다.
> 루트는 STEP 4에서 여기로 리다이렉트합니다.

---

## STEP 2 — 히어로 붙여넣기

**👤 사람이 하는 곳 ②**

1. `/danyang` 페이지 편집 → 맨 위에 `섹션 추가 > 위젯 > HTML`
2. 이 저장소의 **`overnight-hero-block.html` 파일 전체**를 복사해서 붙여넣기
3. 저장 → 미리보기로 확인

**붙여넣고 나서 바로 확인할 것**
- 히어로가 화면 **전체 폭**으로 나오는가 (안 나오면 섹션 설정에서 `전체 너비` 켜기)
- 모바일 미리보기에서 `OVERNIGHT DANYANG` 글자가 안 잘리는가
- 아임웹 테마 CSS와 충돌이 없는가 — 모든 클래스에 `ovn-` 접두사가 붙어 있어 보통은 없습니다

**배경 사진이 생기면**
`overnight-hero-block.html` 안에서 `★ 배경 사진` 이라고 표시된 두 줄의 주석 기호(`/*`, `*/`)만 지우고
`PHOTO_URL_HERE` 를 이미지 주소로 바꾸면 됩니다. 지금은 그라데이션 배경으로도 완성돼 보입니다.

---

## STEP 3 — 나머지 섹션 + 폼

**👤 사람이 하는 곳 ③**

`overnight-imweb-copy.md` 를 열어놓고 위에서부터 순서대로 붙여넣으세요.

| 순서 | 섹션 | 아임웹 블록 | 카피 문서 |
|---|---|---|---|
| 1 | 히어로 | HTML 위젯 | 1번 (STEP 2에서 완료) |
| 2 | 소개 | 텍스트 1단 | 2번 |
| 3 | 프로그램 4축 | 카드 4단 | 3번 · 앵커 `ovn-program` |
| 4 | 타임테이블 | 텍스트 2단 | 4번 |
| 5 | 숙박 | 카드 2단 | 5번 |
| 6 | 주최 실적 | 텍스트 + 숫자 5단 | 6번 |
| 7 | FAQ | 아코디언 | 8번 |
| 8 | 사전 알림 폼 | **폼 블록** · 앵커 `ovn-notify` | **9번** |
| 9 | 푸터 | 기본 푸터 | 10번 |

**폼은 이 순서로 만듭니다**
1. `관리자 > 예약·신청 > 폼 관리 > 폼 만들기`
2. 카피 문서 **9번 표**의 항목 11개를 그대로 생성
3. `폼 설정 > 알림 메일 받을 주소` 에 본인 메일 입력 ← **이거 빠뜨리면 신청이 쌓여도 모릅니다**
4. `/danyang` 페이지에 폼 블록 삽입 → 섹션 앵커 ID를 `ovn-notify` 로 지정
5. 히어로의 `사전 알림 신청` 버튼 링크를 `#ovn-notify` 로 연결

---

## STEP 4 — 루트 리다이렉트

**👤 사람이 하는 곳 ④**

`overnight.asia` 로 들어온 사람을 `/danyang` 으로 보냅니다.

아임웹은 서버 리다이렉트 설정이 플랜/버전마다 다릅니다. 되는 것부터 시도하세요.

**방법 A — 메인페이지 지정 (가장 깔끔)**
`관리자 > 페이지 관리` 에서 `단양` 페이지를 **메인(홈)** 으로 지정.
→ 이러면 `overnight.asia` 와 `overnight.asia/danyang` 이 같은 내용을 보여줍니다.
나중에 브랜드 허브를 만들 때 메인을 허브로 바꾸면 됩니다. **오늘은 이걸 권합니다.**

**방법 B — HTML 위젯 리다이렉트 (A가 안 되면)**
빈 루트 페이지에 HTML 위젯을 넣고 아래를 붙여넣기:

```html
<script>
  if (location.pathname === '/' || location.pathname === '') {
    location.replace('/danyang' + location.search + location.hash);
  }
</script>
<noscript>
  <p style="text-align:center;padding:40px">
    <a href="/danyang">OVERNIGHT DANYANG 2026 페이지로 이동하기</a>
  </p>
</noscript>
```

> 방법 B는 검색엔진 관점에서 301 리다이렉트보다 약합니다. 오래 쓰지 말고 브랜드 허브가 생기면 정리하세요.

---

## STEP 5 — 도메인 연결 ★ 핵심

**👤 사람이 하는 곳 ⑤ (아임웹) + ⑥ (도메인 구매처)**

### 5-1. 아임웹에서 연결 값 확인

1. `관리자 > 설정 > 도메인 > 도메인 연결`
2. `overnight.asia` 입력 → 연결 요청
3. **화면에 표시되는 A레코드(IP) 또는 CNAME 값을 그대로 적어두세요**

> ⚠️ 이 값은 제가 특정해 드릴 수 없습니다. 아임웹은 **계정·플랜·서버군에 따라 값이 다르고**, 인터넷에 돌아다니는 옛날 IP를 쓰면 연결이 안 되거나 나중에 조용히 끊깁니다.
> **반드시 지금 본인 관리자 화면에 뜬 값**을 쓰세요. 블로그·유튜브에 나온 IP를 쓰지 마세요.

### 5-2. 도메인 구매처 DNS에 입력

`overnight.asia` (루트, apex)와 `www.overnight.asia` 둘 다 넣습니다.

| 호스트 | 타입 | 값 | 비고 |
|---|---|---|---|
| `@` (또는 비워둠) | A | 아임웹이 알려준 IP | 루트 도메인 |
| `www` | CNAME | 아임웹이 알려준 주소 | www 접속용 |

**루트에 CNAME은 대부분 안 들어갑니다.** DNS 표준상 apex에는 CNAME을 못 씁니다.
아임웹이 CNAME만 주는 경우, 구매처가 `ALIAS` / `ANAME` / `CNAME 플래트닝` 을 지원하는지 확인하세요.

### 5-확정. 이 프로젝트의 실제 값 ★ 2026-08-06 아임웹 관리자 화면 확인

아임웹 사이트는 `96o32583960.imweb.me` 이고, `overnight.asia` 는 **이미 도메인 목록에 등록**되어
대표 도메인으로 지정된 상태입니다. **연결 방식은 A(네임서버 위임)** 입니다.

**후이즈 `네임서버 관리` 에 넣을 값 — 4개 전부**

```
bns1.hostcocoa.com
bns2.hostcocoa.com
bns3.hostcocoa.com
bns4.hostcocoa.com
```

후이즈가 IP를 함께 요구하면:

| 네임서버 | IP |
|---|---|
| bns1.hostcocoa.com | 205.251.192.254 |
| bns2.hostcocoa.com | 205.251.194.231 |
| bns3.hostcocoa.com | 205.251.196.209 |
| bns4.hostcocoa.com | 205.251.198.107 |

> ⚠️ **4개를 전부 넣으세요.** 1·2차만 넣으면 불안정하거나 거부됩니다.
> 이 값은 아임웹 관리자 `설정 > 도메인` 의 **할당된 네임서버** 칸에서 다시 확인할 수 있습니다.
> 아임웹이 나중에 값을 바꾸면 이 문서가 아니라 관리자 화면 값이 우선입니다.

**반영됐는지 확인** (아임웹 화면 표시는 지연되므로 이걸로 보세요)

```bash
EXPECT_NS="bns1.hostcocoa.com,bns2.hostcocoa.com,bns3.hostcocoa.com,bns4.hostcocoa.com" \
  ./overnight-dns-check.sh
```

> 아임웹 화면의 `현재 네임서버` 칸에는 자체 주석대로 **"최신 네임서버 표시에 시간이 소요"** 됩니다.
> 거기가 아직 `ns1~4.whoisdomain.kr` 로 보여도, 위 명령이 `모두 일치` 라고 하면 실제로는 끝난 겁니다.

---

### 5-0. 연결 방식이 두 가지입니다 — 먼저 어느 쪽인지 정하세요

아임웹이 무엇을 알려줬는지에 따라 갈립니다. **둘을 섞으면 안 됩니다.**

| | 방식 A · 네임서버 위임 | 방식 B · 레코드 직접 입력 |
|---|---|---|
| 아임웹이 준 값 | `ns1...` / `ns2...` 같은 **네임서버 주소** | **IP(A레코드)** 또는 CNAME 주소 |
| 후이즈에서 할 일 | `네임서버 관리` 에서 값 교체 | `DNS 관리` 에서 레코드 추가 |
| DNS를 관리하는 주체 | 아임웹 | 후이즈 |
| 반영 속도 | **느림 — 보통 2~24시간, 최대 48시간** | 빠름 — 보통 10분~1시간 |
| 나중에 메일·다른 서비스 붙일 때 | 아임웹 쪽에서 관리 | 후이즈에서 자유롭게 관리 |

> **방식 A를 선택했다면 후이즈 DNS 관리에 A레코드를 넣지 마세요.** 네임서버를 위임한 순간
> 후이즈의 DNS 레코드는 더 이상 사용되지 않습니다. 반대로 방식 B라면 네임서버는 후이즈 것 그대로 둬야 합니다.

---

### 5-1-A. 방식 A(네임서버 위임)인데 아임웹에 표시가 안 될 때 ★

네임서버를 바꿨는데 아임웹 관리자에 연결됨 표시가 안 뜨는 경우, 원인은 대개 아래 넷 중 하나입니다.
**위에서부터 순서대로** 확인하세요.

**① 아임웹에 도메인을 "추가"했습니까?**

네임서버만 바꾸면 아임웹은 그 도메인이 자기 것인지 **모릅니다.**
`관리자 > 설정 > 도메인` 에서 `overnight.asia` 를 직접 입력해 **등록**하는 절차가 따로 있어야 합니다.
이걸 안 하면 네임서버가 아무리 잘 붙어도 화면에 영원히 안 뜹니다. 제일 흔한 원인입니다.

**② 아직 전파 중입니다 (가장 흔한 정상 상황)**

네임서버 변경은 레코드 변경보다 훨씬 느립니다. **최소 2시간, 보통 반나절, 최대 48시간**입니다.
바꾸자마자 확인하면 당연히 안 나옵니다.

전파가 실제로 어디까지 왔는지는 아래로 확인하세요. 레지스트리에 반영됐는지를 직접 조회합니다.

```bash
# 아임웹이 준 네임서버 값을 그대로 넣으세요
EXPECT_NS="ns1.아임웹이준값,ns2.아임웹이준값" ./overnight-dns-check.sh
```

결과 해석:

| 출력 | 뜻 | 할 일 |
|---|---|---|
| `기대한 네임서버와 모두 일치합니다` | 레지스트리 반영 완료 | 아임웹이 인식할 때까지 대기 (①도 확인) |
| `공용 리졸버 값과 다릅니다 — 캐시` | 레지스트리는 됐고 캐시만 남음 | **기다리면 됩니다** |
| `기대값 누락` | 아직 레지스트리에 안 올라감 | ③으로 |
| `레지스트리에 위임 정보가 없습니다` | 후이즈가 제출을 안 함 | ③으로 |

**③ 후이즈에서 변경이 실제로 "신청"됐는지**

후이즈는 네임서버를 입력만 하고 **`적용` / `신청` 버튼을 한 번 더 눌러야** 제출되는 화면이 있습니다.
`도메인 관리 > 네임서버 관리` 를 다시 열어 값이 실제로 바뀌어 저장돼 있는지 눈으로 확인하세요.
일부 변경은 **휴대폰 본인인증**을 요구하며, 인증을 안 끝내면 접수만 되고 반영되지 않습니다.

**④ 값 오타 — 특히 흔한 것**

- 네임서버는 보통 **2개 이상 전부** 넣어야 합니다. 하나만 넣으면 거부되거나 불안정합니다.
- 앞뒤 공백, `http://` 를 붙인 경우, 끝에 점(`.`) 유무
- 아임웹 화면의 값을 **손으로 옮겨 적지 말고 복사·붙여넣기** 하세요

**그래도 12시간 넘게 안 되면** — 후이즈 고객센터에 "네임서버 변경이 레지스트리에 반영됐는지"를,
아임웹 고객센터에 "도메인 연결 상태"를 각각 문의하세요. 양쪽 다 자기 쪽만 볼 수 있습니다.

---

### 5-2-후이즈. 후이즈에서 DNS 입력 (방식 B) ★ 이 도메인은 후이즈에서 구매했습니다

**① 네임서버가 후이즈 것으로 되어 있는지 먼저 확인**

`후이즈 로그인 > 마이후이즈 > 도메인 > 도메인 관리 > overnight.asia 선택 > 네임서버 관리`

**판단 기준은 특정 주소가 아니라 "후이즈 소유의 네임서버인가"** 입니다.
후이즈 기본 네임서버는 아래 형태입니다. (2026-08 기준 실제 확인값)

```
ns1.whoisdomain.kr
ns2.whoisdomain.kr
```

> 후이즈는 시기·상품에 따라 `whois.co.kr` 등 다른 도메인의 네임서버를 쓰기도 합니다.
> 문자열을 외워서 대조하지 마시고, **`DNS 관리` 메뉴가 정상적으로 열리는지**로 판단하세요.
> 메뉴가 열리고 레코드 목록이 보이면 그대로 두시면 됩니다.

✅ **이미 후이즈 네임서버로 되어 있다면 이 단계는 건너뛰고 ②로 바로 가세요.**

바꿔야 하는 경우는 예전 호스팅사 NS나 외부 NS(예: Cloudflare)로 되어 있을 때뿐입니다.
그 경우 후이즈 기본 네임서버로 되돌리면 되고, **네임서버 변경은 그 자체로 전파에 몇 시간
걸릴 수 있으니 가장 먼저** 처리하세요.

**② DNS 레코드 추가**

`도메인 관리 > overnight.asia > DNS 관리 (또는 DNS 정보 관리)`

| 호스트 | 타입 | 값 |
|---|---|---|
| (비워둠) 또는 `@` | A | 아임웹 화면의 IP |
| `www` | CNAME | 아임웹 화면의 주소 |

**후이즈에서 자주 걸리는 것**

- 후이즈는 호스트 칸을 **비워두는 것**이 루트(`@`)를 뜻하는 경우가 많습니다. `@` 를 넣어 저장이 안 되면 비우고 다시 시도하세요.
- CNAME 값 끝에 **점(`.`)** 을 붙일지 말지가 화면마다 다릅니다. 저장 후 목록에 표시된 값을 보고, 점이 두 번 붙거나(`..`) 도메인이 덧붙지 않았는지 확인하세요.
- 구매 직후 깔려 있는 **파킹 페이지용 A 레코드를 먼저 삭제**하세요. 안 지우면 아임웹 IP와 충돌해서 접속이 왔다 갔다 합니다.
- 후이즈는 **저장 후 별도의 `적용`/`확인` 버튼**을 한 번 더 눌러야 반영되는 화면이 있습니다. 목록에서 값이 실제로 바뀌었는지 눈으로 확인하세요.
- 아임웹이 CNAME만 주는데 루트에 안 들어가면 — DNS 표준상 apex에는 CNAME을 못 씁니다. 아임웹에 A레코드(IP)를 달라고 하거나, 아래 Cloudflare 방식으로 넘어가세요.

**③ 반영 확인**

```bash
./overnight-dns-check.sh --watch
```

---

**다른 구매처를 쓰게 될 경우 (참고)**

- **가비아** : `My가비아 > 서비스 관리 > DNS 관리 > 레코드 수정`.
- **Cloudflare** : ⚠️ **프록시(주황색 구름)를 반드시 꺼서 `DNS only`(회색 구름)로 두세요.**
  주황색이면 아임웹이 SSL 인증서를 발급하지 못해 무한 대기에 걸립니다. SSL 발급 완료 후에도 켜지 마세요 — 이중 SSL로 꼬입니다.
  Cloudflare는 루트 CNAME 플래트닝을 지원하므로 apex에 CNAME을 넣어도 됩니다.
- **Namecheap / GoDaddy** : `Advanced DNS` / `DNS 관리` 에서 A · CNAME 추가.
  기본 **파킹 페이지 레코드를 먼저 삭제**하세요.

> **DNS를 자동화하고 싶다면** — 후이즈에서 네임서버만 Cloudflare로 옮기면(무료) Cloudflare는 실제 API가 있어
> `Zone:DNS:Edit` 스코프 토큰으로 레코드 생성을 자동화할 수 있습니다.
> 계정 비밀번호가 아니라 이 도메인에만 유효하고 언제든 취소 가능한 토큰이라 안전합니다.
> 단, 아임웹 관리자 설정은 **API가 없어 어떤 방법으로도 자동화되지 않습니다.**

### 5-3. 전파 대기

보통 몇 분 ~ 수 시간. 최대 48시간까지 볼 수 있지만 실제로는 대부분 1시간 안에 끝납니다.

**→ 검증 (자동)**

```bash
./overnight-dns-check.sh
```

DNS · HTTPS · 리다이렉트 · SSL 만료일까지 한 번에 확인합니다. 반복해서 돌리려면:

```bash
./overnight-dns-check.sh --watch      # 60초마다 재확인
```

---

## STEP 6 — SSL · 오픈

**👤 사람이 하는 곳 ⑥**

### ⚠️ 아임웹 개인 도메인은 SSL이 자동 적용되지 않습니다

아임웹 관리자 화면의 안내 그대로입니다.

> 아임웹 기본 도메인(예: mysite.imweb.me)이 아닌 **개인 도메인(예: mysite.com)은 보안서버(SSL)가
> 자동 적용되지 않아 별도 신청이 필요합니다.**

**우리 페이지는 SSL이 선택이 아니라 의무입니다.** 사전 알림 폼에서 이름·휴대폰·이메일을 받기 때문입니다.
아임웹 안내에 따르면 개인정보를 취급하는 웹사이트는 보안서버 의무화 대상이고 과태료 대상이 될 수 있습니다.
SSL 없이 개인정보 폼을 열지 마세요.

### 순서

1. **네임서버 위임이 끝났는지 먼저 확인** (STEP 5-확정의 `EXPECT_NS` 명령)
2. `관리자 > 설정 > 도메인` 의 **`보안서버(SSL) 신청 및 상세정보`** 링크에서 **SSL을 직접 신청**
   - 플랜에 따라 유료일 수 있습니다. 비용·소요 기간은 신청 화면에서 확인하세요
   - 네임서버가 아임웹으로 넘어온 뒤에 신청해야 검증이 통과합니다
3. 발급 완료 후 `https://overnight.asia` 로 접속해 자물쇠 표시 확인
4. `대표 도메인으로 자동연결` 체크 (이미 켜져 있음) — 임시 주소로 들어와도 본 도메인으로 넘어갑니다
5. **HTTPS 강제 리다이렉트** 옵션이 있으면 켜세요
6. 사이트 상태를 `오픈` 으로 전환

### www 로도 접속되게 하려면

관리자 도메인 화면의 **`개인 도메인에 www 포함하기`** 체크박스를 켜세요. (현재 꺼져 있습니다)
끄면 `www.overnight.asia` 로 들어온 사람은 접속이 안 됩니다.
켤 경우 SSL도 www 를 포함해 발급되는지 신청 화면에서 확인하세요.

### SSL이 안 나올 때 체크리스트

- **SSL을 신청은 했는가** — 아임웹 개인 도메인은 자동이 아닙니다. 제일 흔한 원인입니다
- 네임서버 위임이 실제로 끝났는가 (`EXPECT_NS` 명령으로 확인)
- CAA 레코드가 걸려 있지 않은가 — 있으면 삭제하거나 발급 CA를 허용
- 예전 호스팅의 A 레코드가 남아 있지 않은가

---

## 오픈 후 (도메인 붙은 뒤에 해야 하는 것들)

- [ ] 네이버 서치어드바이저 사이트 등록 + 소유확인 (`관리자 > 마케팅 > 검색등록`)
- [ ] 구글 서치콘솔 등록
- [ ] 카카오톡으로 링크 보내서 OG 미리보기 확인
- [ ] 인스타 프로필 링크를 `https://overnight.asia/danyang` 로 교체
- [ ] 폼 테스트 제출 1회 → 알림 메일 도착 확인
- [ ] `overnight-imweb-copy.md` 14번 체크리스트 전체 실행

---

## 제가 자동으로 못 하는 것 · 이유

### ⚠️ 계정 비밀번호는 보내지 마세요

자격 증명을 주셔도 **작업이 되지 않습니다.** 이 작업 환경의 네트워크 정책이 외부 호스트 접속을 차단합니다.
실제 확인 결과:

```
imweb.me:443            403 (policy denial)
api.imweb.me:443        403
whois.co.kr:443         403
api.cloudflare.com:443  403
```

로그인 화면에 도달조차 못 하므로, 비밀번호는 위험만 남기고 아무것도 해결하지 못합니다.
후이즈 계정은 **도메인 소유권 자체**를, 아임웹 계정은 **결제 정보**를 쥐고 있습니다.

### 막힌 층이 네 겹입니다

| 장벽 | 해결 가능? |
|---|---|
| 이 환경의 네트워크 정책 | 사용자가 환경 설정에서 변경 시 가능 |
| 아임웹에 페이지·도메인 설정 API 없음 (오픈API는 주문·상품 위주) | 불가 |
| 후이즈·아임웹 로그인의 휴대폰 본인인증 | 불가 |
| 자격 증명이 대화 기록에 남는 문제 | 안 보내면 해결 |

네트워크를 여신다 해도 2·3번이 남습니다. **아임웹 연동은 누가 하든 수동입니다.**
DNS만은 Cloudflare 네임서버 + 스코프 토큰으로 자동화 여지가 있습니다(5-2 마지막 참고).

**제가 한 것** — 붙여넣을 파일 전부, 후이즈 기준 클릭 순서, 전파·SSL 자동 검증 스크립트.
관리자 화면 6곳만 눌러주시면 됩니다.
