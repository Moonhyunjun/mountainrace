# overnight.asia — 도메인 · 아임웹 연동 런북

오늘 안에 끝내는 순서입니다. **사람이 관리자 화면에서 눌러야 하는 지점은 6곳뿐**이고, 나머지는 이 저장소 파일 복붙으로 끝납니다.
각 단계 끝의 `→ 검증` 은 `./overnight-dns-check.sh` 로 자동 확인할 수 있습니다.

---

## 시작 전 확인 (2분)

- [ ] `overnight.asia` 를 **어디서 샀는지** — 가비아 / 후이즈 / Cloudflare / Namecheap / GoDaddy 중 어디인지
- [ ] 그 계정에 **로그인이 되는지** (DNS 관리 화면까지 들어가지는지)
- [ ] 아임웹 요금제가 **유료 플랜인지** — ⚠️ 무료 플랜은 개인 도메인 연결이 안 됩니다. 결제하셨다니 통과일 겁니다
- [ ] 아임웹 사이트가 **"오픈" 상태인지** (비공개 상태면 도메인을 연결해도 접속이 막힙니다)

---

## STEP 1 — 아임웹 템플릿 선택 · 페이지 골격 만들기

**👤 사람이 하는 곳 ①**

1. 아임웹 관리자 → `디자인 > 디자인 관리 > 템플릿 변경`
2. **이벤트 / 랜딩 / 원페이지** 계열에서 **다크 배경을 지원하는** 템플릿 선택
   - 사이드바 없는 **원페이지형**을 고르세요. 히어로가 전체 폭으로 나와야 합니다
   - 상단 고정 헤더(스티키)가 되는 템플릿이면 더 좋습니다
3. `페이지 관리 > 페이지 추가`
   - 페이지 이름: `단양`
   - 페이지 주소: `danyang`  → 최종 주소가 `overnight.asia/danyang` 이 됩니다
   - 메뉴 노출: **끄기**
4. 페이지 배경색을 `#080b11` 로 지정 (`디자인 > 색상`)

> 왜 `/danyang` 인가: 2027 문경이 붙을 때 URL을 갈아엎지 않기 위해서입니다.
> 루트는 STEP 4에서 여기로 리다이렉트합니다.

---

## STEP 2 — 히어로 붙여넣기

**👤 사람이 하는 곳 ②**

1. `/danyang` 페이지 편집 → 맨 위에 `섹션 추가 > 위젯 > HTML`
2. 이 저장소의 **`overnight-hero-block.html` 파일 전체**를 복사해서 붙여넣기
3. 저장 → 미리보기로 확인

**붙여넣고 나서 바로 확인할 것**
- 히어로가 화면 **전체 폭**으로 나오는가 (안 나오면 섹션 설정에서 `전체 너비` 켜기)
- 모바일 미리보기에서 `OVERNIGHT DANYANG` 글자가 안 잘리는가
- 아임웹 테마 CSS와 충돌이 없는가 — 모든 클래스에 `ovn-` 접두사가 붙어 있어 보통은 없습니다

**배경 사진이 생기면**
`overnight-hero-block.html` 안에서 `★ 배경 사진` 이라고 표시된 두 줄의 주석 기호(`/*`, `*/`)만 지우고
`PHOTO_URL_HERE` 를 이미지 주소로 바꾸면 됩니다. 지금은 그라데이션 배경으로도 완성돼 보입니다.

---

## STEP 3 — 나머지 섹션 + 폼

**👤 사람이 하는 곳 ③**

`overnight-imweb-copy.md` 를 열어놓고 위에서부터 순서대로 붙여넣으세요.

| 순서 | 섹션 | 아임웹 블록 | 카피 문서 |
|---|---|---|---|
| 1 | 히어로 | HTML 위젯 | 1번 (STEP 2에서 완료) |
| 2 | 소개 | 텍스트 1단 | 2번 |
| 3 | 프로그램 4축 | 카드 4단 | 3번 · 앵커 `ovn-program` |
| 4 | 타임테이블 | 텍스트 2단 | 4번 |
| 5 | 숙박 | 카드 2단 | 5번 |
| 6 | 주최 실적 | 텍스트 + 숫자 5단 | 6번 |
| 7 | FAQ | 아코디언 | 8번 |
| 8 | 사전 알림 폼 | **폼 블록** · 앵커 `ovn-notify` | **9번** |
| 9 | 푸터 | 기본 푸터 | 10번 |

**폼은 이 순서로 만듭니다**
1. `관리자 > 예약·신청 > 폼 관리 > 폼 만들기`
2. 카피 문서 **9번 표**의 항목 11개를 그대로 생성
3. `폼 설정 > 알림 메일 받을 주소` 에 본인 메일 입력 ← **이거 빠뜨리면 신청이 쌓여도 모릅니다**
4. `/danyang` 페이지에 폼 블록 삽입 → 섹션 앵커 ID를 `ovn-notify` 로 지정
5. 히어로의 `사전 알림 신청` 버튼 링크를 `#ovn-notify` 로 연결

---

## STEP 4 — 루트 리다이렉트

**👤 사람이 하는 곳 ④**

`overnight.asia` 로 들어온 사람을 `/danyang` 으로 보냅니다.

아임웹은 서버 리다이렉트 설정이 플랜/버전마다 다릅니다. 되는 것부터 시도하세요.

**방법 A — 메인페이지 지정 (가장 깔끔)**
`관리자 > 페이지 관리` 에서 `단양` 페이지를 **메인(홈)** 으로 지정.
→ 이러면 `overnight.asia` 와 `overnight.asia/danyang` 이 같은 내용을 보여줍니다.
나중에 브랜드 허브를 만들 때 메인을 허브로 바꾸면 됩니다. **오늘은 이걸 권합니다.**

**방법 B — HTML 위젯 리다이렉트 (A가 안 되면)**
빈 루트 페이지에 HTML 위젯을 넣고 아래를 붙여넣기:

```html
<script>
  if (location.pathname === '/' || location.pathname === '') {
    location.replace('/danyang' + location.search + location.hash);
  }
</script>
<noscript>
  <p style="text-align:center;padding:40px">
    <a href="/danyang">OVERNIGHT DANYANG 2026 페이지로 이동하기</a>
  </p>
</noscript>
```

> 방법 B는 검색엔진 관점에서 301 리다이렉트보다 약합니다. 오래 쓰지 말고 브랜드 허브가 생기면 정리하세요.

---

## STEP 5 — 도메인 연결 ★ 핵심

**👤 사람이 하는 곳 ⑤ (아임웹) + ⑥ (도메인 구매처)**

### 5-1. 아임웹에서 연결 값 확인

1. `관리자 > 설정 > 도메인 > 도메인 연결`
2. `overnight.asia` 입력 → 연결 요청
3. **화면에 표시되는 A레코드(IP) 또는 CNAME 값을 그대로 적어두세요**

> ⚠️ 이 값은 제가 특정해 드릴 수 없습니다. 아임웹은 **계정·플랜·서버군에 따라 값이 다르고**, 인터넷에 돌아다니는 옛날 IP를 쓰면 연결이 안 되거나 나중에 조용히 끊깁니다.
> **반드시 지금 본인 관리자 화면에 뜬 값**을 쓰세요. 블로그·유튜브에 나온 IP를 쓰지 마세요.

### 5-2. 도메인 구매처 DNS에 입력

`overnight.asia` (루트, apex)와 `www.overnight.asia` 둘 다 넣습니다.

| 호스트 | 타입 | 값 | 비고 |
|---|---|---|---|
| `@` (또는 비워둠) | A | 아임웹이 알려준 IP | 루트 도메인 |
| `www` | CNAME | 아임웹이 알려준 주소 | www 접속용 |

**루트에 CNAME은 대부분 안 들어갑니다.** DNS 표준상 apex에는 CNAME을 못 씁니다.
아임웹이 CNAME만 주는 경우, 구매처가 `ALIAS` / `ANAME` / `CNAME 플래트닝` 을 지원하는지 확인하세요.

**구매처별 메모**

- **가비아** : `My가비아 > 서비스 관리 > DNS 관리 > 레코드 수정`.
  호스트 `@` 와 `www` 각각 추가. 저장 후 반영까지 보통 10분~1시간.
- **후이즈** : `도메인 관리 > DNS 관리`. 네임서버가 후이즈 기본 NS로 되어 있어야 DNS 관리 메뉴가 열립니다.
- **Cloudflare** : ⚠️ **프록시(주황색 구름)를 반드시 꺼서 `DNS only`(회색 구름)로 두세요.**
  주황색이면 아임웹이 SSL 인증서를 발급하지 못해 무한 대기에 걸립니다. SSL 발급 완료 후에도 켜지 마세요 — 이중 SSL로 꼬입니다.
  Cloudflare는 루트 CNAME 플래트닝을 지원하므로 apex에 CNAME을 넣어도 됩니다.
- **Namecheap / GoDaddy** : `Advanced DNS` / `DNS 관리` 에서 A · CNAME 추가.
  기본으로 깔려 있는 **파킹 페이지 레코드(A 레코드나 CNAME `parkingpage`)를 먼저 삭제**하세요. 안 지우면 충돌합니다.

### 5-3. 전파 대기

보통 몇 분 ~ 수 시간. 최대 48시간까지 볼 수 있지만 실제로는 대부분 1시간 안에 끝납니다.

**→ 검증 (자동)**

```bash
./overnight-dns-check.sh
```

DNS · HTTPS · 리다이렉트 · SSL 만료일까지 한 번에 확인합니다. 반복해서 돌리려면:

```bash
./overnight-dns-check.sh --watch      # 60초마다 재확인
```

---

## STEP 6 — SSL · 오픈

**👤 사람이 하는 곳 ⑥**

1. DNS가 잡히면 아임웹이 **자동으로 SSL 인증서를 발급**합니다 (보통 10분~1시간)
2. `관리자 > 설정 > 도메인` 에서 SSL 상태가 `발급 완료` 로 바뀌는지 확인
3. `설정 > 도메인 > 기본 도메인` 을 `overnight.asia` 로 지정
   → 아임웹 임시 주소(`xxx.imweb.me`)로 들어와도 본 도메인으로 넘어갑니다
4. **HTTPS 강제 리다이렉트** 옵션이 있으면 켜세요
5. 사이트 상태를 `오픈` 으로 전환

**SSL이 안 나올 때 체크리스트**
- Cloudflare 프록시가 켜져 있지 않은가 (제일 흔한 원인)
- CAA 레코드가 걸려 있지 않은가 — 있으면 삭제하거나 `letsencrypt.org` 를 허용
- DNS 값이 아임웹 화면의 값과 정확히 같은가 (뒤에 점 하나 차이로도 실패합니다)
- 예전 호스팅의 A 레코드가 남아 있지 않은가

---

## 오픈 후 (도메인 붙은 뒤에 해야 하는 것들)

- [ ] 네이버 서치어드바이저 사이트 등록 + 소유확인 (`관리자 > 마케팅 > 검색등록`)
- [ ] 구글 서치콘솔 등록
- [ ] 카카오톡으로 링크 보내서 OG 미리보기 확인
- [ ] 인스타 프로필 링크를 `https://overnight.asia/danyang` 로 교체
- [ ] 폼 테스트 제출 1회 → 알림 메일 도착 확인
- [ ] `overnight-imweb-copy.md` 14번 체크리스트 전체 실행

---

## 제가 자동으로 못 하는 것 · 이유

| 항목 | 이유 |
|---|---|
| 아임웹 관리자 설정 | 공개 API로 페이지·도메인을 설정하는 경로가 없습니다. 계정 로그인도 이 세션에 없습니다 |
| DNS 레코드 입력 | 도메인 구매처 계정 접근 권한이 없습니다 |
| A레코드 값 특정 | 아임웹 계정·플랜마다 달라, 추측해서 넣으면 조용히 끊깁니다 |
| 인스타 핸들 선점 | 계정 생성은 본인 인증이 필요합니다 |

**제가 한 것** — 붙여넣을 파일 전부, 클릭 순서 특정, 전파·SSL 자동 검증 스크립트.
관리자 화면 6곳만 눌러주시면 됩니다.
